server:
  port: 9092

spring:
  cloud:
    discovery:
      enabled: true
  main:
    allow-circular-references: true
  application:
    name: basket
  datasource:
    url: jdbc:mysql://localhost:3306/trendyol_basket?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: mustafa123
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


